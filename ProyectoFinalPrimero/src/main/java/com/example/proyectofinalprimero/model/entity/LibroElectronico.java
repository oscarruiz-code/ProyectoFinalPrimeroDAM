package com.example.proyectofinalprimero.model.entity;

/**
 * Clase que representa un libro electrónico y extiende de la clase Libro.
 *
 * @version 1.0
 * @autor oscarruiz-code
 */

/*
public class LibroElectronico extends Libro {

    private StringProperty formato;
    private StringProperty tamanoArchivo;

    public LibroElectronico(String titulo, String autor, String genero, Long isbn, Integer publicacion, String formato, String tamanoArchivo) {
        super(titulo, autor, genero, isbn, publicacion);
        this.formato = new SimpleStringProperty(formato);
        this.tamanoArchivo = new SimpleStringProperty(tamanoArchivo);
    }

    // Métodos getter y setter para formato
    public String getFormato() {
        return formato.get();
    }

    public void setFormato(String formato) {
        this.formato.set(formato);
    }

    public StringProperty formatoProperty() {
        return formato;
    }

    // Métodos getter y setter para tamanoArchivo
    public String getTamanoArchivo() {
        return tamanoArchivo.get();
    }

    public void setTamanoArchivo(String tamanoArchivo) {
        this.tamanoArchivo.set(tamanoArchivo);
    }

    public StringProperty tamanoArchivoProperty() {
        return tamanoArchivo;
    }
}
*/